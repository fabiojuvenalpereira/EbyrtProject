const database = 'ebytr';
const collection = 'todoList';
use(database);
db.createCollection(collection);

use('ebytr');
db.todoList.insertOne({
  userName: 'fabio',
  tarefa: 'criar banco de dados',
  createdDate: Date.now(),
  updateDate: '',
  status: 'pending',
});



use('ebytr');
db.todoList.find({});

use('ebytr');
db.todoList.deleteMany({});